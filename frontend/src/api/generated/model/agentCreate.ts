/**
 * Generated by orval v8.3.0 🍺
 * Do not edit manually.
 * AxiaCraft Product Foundry API
 * OpenAPI spec version: 0.1.0
 */
import type { AgentCreateAuthProfile } from "./agentCreateAuthProfile";
import type { AgentCreateHeartbeatConfig } from "./agentCreateHeartbeatConfig";
import type { AgentCreateIdentityProfile } from "./agentCreateIdentityProfile";
import type { AgentCreateSkillEnv } from "./agentCreateSkillEnv";

/**
 * Payload for creating a new agent.
 */
export interface AgentCreate {
  /** Agent type: board_worker, board_lead, gateway_main, or standalone. */
  agent_type?: string;
  /** Full auth-profiles.json content written to the agent workspace at provision time. Supports multiple providers (Anthropic, GitHub Copilot, etc.). Written as auth-profiles.json — not stored on the agent record. Example: {"version": 1, "profiles": {"anthropic:main": {"type": "token", "provider": "anthropic", "token": "sk-ant-..."}}, "order": {"anthropic": ["anthropic:main"]}, "lastGood": {}, "usageStats": {}}. */
  auth_profile?: AgentCreateAuthProfile;
  /** Board id that scopes this agent. Omit only when policy allows global agents. */
  board_id?: string | null;
  /** Gateway UUID for standalone agents. Required when agent_type is 'standalone'. Ignored for board-scoped agents (gateway is derived from the board). */
  gateway_id?: string | null;
  /** Runtime heartbeat behavior overrides for this agent. */
  heartbeat_config?: AgentCreateHeartbeatConfig;
  /** Optional profile hints used by routing and policy checks. */
  identity_profile?: AgentCreateIdentityProfile;
  /** Template that helps define initial intent and behavior. */
  identity_template?: string | null;
  /** Per-agent skill allowlist pushed to gateway agents.list[].skills. None = inherit gateway defaults. [] = no skills. ["a","b"] = explicit list. */
  installed_skills?: string[] | null;
  /** Whether this agent should be created as the board lead. When true, the agent will be assigned the board lead session key and role. Only one board lead is allowed per board. */
  is_board_lead?: boolean;
  /**
   * Human-readable agent display name.
   * @minLength 1
   */
  name: string;
  /** Per-skill credential overrides written to skills/<slug>/config.env at provision time. Example: {"plane-workflow": {"PLANE_API_KEY": "plane_api_xxx"}}. Values are write-only and not stored on the agent record. */
  skill_env?: AgentCreateSkillEnv;
  /** Template representing deeper agent instructions. */
  soul_template?: string | null;
  /** Current lifecycle state used by coordinator logic. */
  status?: string;
  /** Optional additional tool documentation appended to TOOLS.md at provision time. Use for custom API instructions, endpoints, or usage notes specific to this agent. Write-only — not stored on the agent record. */
  tool_instructions?: string | null;
}
